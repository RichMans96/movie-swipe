import React from 'react';
import { LoaderContainer, LoaderText, LoaderWrapper } from './MiscElements';

const Loader = () => {
 return (
  <LoaderContainer>
   <LoaderWrapper>
    <LoaderText>Loading!</LoaderText>
   </LoaderWrapper>
  </LoaderContainer>
 );
};

export default Loader;
